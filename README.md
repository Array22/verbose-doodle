To build:
1. Make sure current directory is that of parent folder 'verbose-doodle' or 'verbose-doodle-main'
2. dotnet build
3. dotnet run (this will automatically host on local server and open window to application)

To test (Test files are in Test.cs):
1. dotnet test