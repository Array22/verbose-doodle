To build:
1. Make sure .NET 10 is installed.
2. git clone
3. Make sure current directory is that of parent folder 'verbose-doodle'
4. dotnet build
5. dotnet run (this will automatically host on local server and open window to application)

To test (Test files are in Test.cs):
1. dotnet test

Solution Explanation in Word file.
